/**
 * Re-exports the static exercise bank from the project root so it is
 * importable inside src/ via the @/ path alias.
 *
 * Usage: import { EXERCISES, SECTION_CONFIG } from '@/lib/exercises'
 */
export { EXERCISES } from '../../tef-exercises'
export type { TEFExercise } from '../../tef-exercises'
